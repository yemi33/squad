# Dallas Learnings — 2026-03-12

## Task: Add cowork route to bebop webapp

### Findings
- Bebop uses TanStack Router with file-based routing under `apps/bebop/src/routes/`
- Route files follow `_mainLayout.<routeName>.tsx` naming convention
- `routeTree.gen.ts` is auto-generated but committed to git — needs manual update if build can't be run
- The repo uses CRLF line endings on Windows — must account for this when doing programmatic edits
- Yarn install in worktrees takes very long (~200 packages monorepo) — consider reusing main tree's node_modules

### Reference
- PR 4855722 (commit a33692e) is the workspace routes pattern to follow
- New PR: 4959092 (dallas/add-cowork-route branch)

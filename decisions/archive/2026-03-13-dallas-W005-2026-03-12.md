# Dallas W005 — Build/Test Findings: dallas/add-cowork-route

**Date:** 2026-03-12
**Task:** Build Dallas cowork PR locally and verify it works
**Branch:** `dallas/add-cowork-route` (office-bohemia)
**PR:** PR-4959180

## Build Status: ❌ FAIL — Node.js version mismatch

### Root Cause
The office-bohemia repo requires **Node.js >=24.11.0** (per `package.json` engines and `.nvmrc`), but this machine only has **Node.js 20.12.2** installed. There is no Node version manager (nvm, fnm, volta) available on this DevBox.

### What Happened

1. **Worktree created** at `C:/Users/yemishin/worktrees/test-W005` from `origin/dallas/add-cowork-route` ✅
2. **`yarn install`** — Dependency resolution and linking succeeded, but the `afterInstall` hook failed with:
   ```
   Error: Node.js version 20.12.2 does not satisfy the required version >=24.11.0.
   ```
   `node_modules` were created despite this.
3. **`yarn build --to bebop`** — Failed at `@fluidx/eslint-plugin-ffx-rules preTranspile` due to ESM/CJS incompatibility with Node 20:
   ```
   Error [ERR_REQUIRE_ESM]: require() of ES Module just.config.js not supported.
   ```
4. **`yarn dev` (apps/bebop)** — Vite briefly started on `http://localhost:3000/bebop/` but exited because:
   - Vite 7.3.1 requires Node.js 20.19+ or 22.12+ (current is 20.12.2)
   - Auth proxy failed: SSL certificates not set up

### Code Review (visual inspection)

The route file `apps/bebop/src/routes/_mainLayout.cowork.tsx` looks correct:
- Follows the thin-route pattern (head, loader, component)
- Uses `createFileRoute('/_mainLayout/cowork')` properly
- Loads translations for CopilotChatInput namespaces
- Renders `<Chat />` inside `<RealtimeVoiceProvider>`
- Includes resetToken for message reset on navigation

The `routeTree.gen.ts` was also updated (auto-generated by TanStack Router).

### Required Action

To build and run this PR locally, this DevBox needs:
1. **Node.js 24.11.0+** installed (the minimum version per `package.json` engines)
2. **SSL certificates** set up for the auth proxy (see bebop README)
3. A Node version manager (fnm or nvm-windows) would help for switching between versions

### Localhost URL (if it had worked)
`http://localhost:3000/bebop/cowork` (the `/cowork` route under the main layout)

## Learnings

- **office-bohemia now requires Node.js 24+** — this is a hard requirement enforced by both `package.json` engines, the yarn afterInstall hook, and Vite's own version check
- The DevBox at `yemishin` only has Node 20.12.2 with no version manager
- Even with the wrong Node version, `yarn install` partially succeeds (node_modules are created) but the afterInstall hook and build tasks fail
- Vite dev server starts on port 3000 with base path `/bebop/`

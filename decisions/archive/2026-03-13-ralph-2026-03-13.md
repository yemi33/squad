# Ralph — PR Sync Learnings

**Date:** 2026-03-13
**Task:** PR status sync for all active PRs

## Findings

1. **PR-4959092** (Add cowork route) — Still active, no reviewer votes, merge succeeds cleanly. No changes from local tracker.
2. **PR-4959405** (W003 Design doc) — Still active, no reviewer votes, merge succeeds cleanly. No changes from local tracker.

## Observations

- Both PRs only have the "Bebop PR Reviewers" group assigned (auto-assigned by ADO policy), with vote=0. No individual human reviewers have been added yet.
- Both PRs have `mergeStatus: 3` meaning they merge cleanly against master — no conflicts.
- ADO status codes: `status: 1`=active, `2`=abandoned, `3`=completed. `mergeStatus: 3`=succeeded (clean merge possible).
- The `lastMergeCommit` field is ADO's test-merge commit (to check mergeability), not an actual merge into master.

## Patterns

- PR sync is straightforward: fetch via `mcp__azure-ado__repo_get_pull_request_by_id` with repositoryId + numeric PR ID, then map fields to local tracker format.
- Reviewer vote mapping: 10=approved, 5=approved-with-suggestions, -5=waiting, -10=rejected, 0=no-vote.

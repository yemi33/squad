# M005: DataWorks HR/People Operations Agent (dw-hr-agent)

**Author:** Dallas (Engineer)
**Date:** 2026-03-11
**Status:** Complete
**Branch:** `feature/m005-dw-hr-agent`

## What was done

Created a new DataWorks agent `dw-hr-agent` for HR/People Operations scenarios following the `dw-marketer-agent` pattern exactly.

## Files created

### Agent package (`agents/dw-hr-agent/`)
1. `package.json` -- `@officeagent/dw-hr-agent` with same deps as dw-marketer-agent
2. `tsconfig.json`, `gulpfile.mjs`, `jest.config.js` -- standard monorepo config files
3. `CLAUDE.md` -- developer documentation

### Source files (`src/`)
4. `registry.ts` -- agent config: id `dw-hr-agent`, Claude Sonnet model, version 1.0.0, 5 skills, 4 subagents, flight overrides for Opus 4.5/4.6
5. `dw-hr-agent.ts` -- main `DWHRAgent` class following dw-marketer-agent pattern
6. `index.ts` -- package exports
7. `claude-message-handler.ts` -- SDK message handler (identical pattern)
8. `ws-request-manager.ts` -- WebSocket request/response manager
9. `upload-file-to-share-point-tool.ts` -- SharePoint upload MCP tool

### Prompts (`src/prompts/1.0.0/`)
10. `system-prompt.md` -- HR-focused system prompt covering 5 scenarios: job descriptions, performance reviews, onboarding materials, org charts, HR policies
11. `rai.md` -- HR-specific RAI guidelines (employment law, PII, bias/fairness, confidentiality)

### Skills (copied from existing agents)
- `docx/` -- from dw-marketer-agent
- `pptx/` -- from dw-marketer-agent
- `enterprise-file-search/` -- from dw-marketer-agent
- `email/` -- from dw-marketer-agent
- `email-formatter/` -- from dw-paralegal-agent 1.0.1

### Subagents (copied from dw-marketer-agent)
- `file-download.md`
- `communication-subagent.md`
- `docx-outline-generator.md`
- `docx-section-generator.md`

### Scripts (copied from dw-marketer-agent)
- `enterprise-search.js`, `enterprise-fetch.js`, `fetch-user-context.js`

## Shared file modifications

12. `modules/api/src/agents.ts` -- imported and registered `DWHRAgent`
13. `modules/api/package.json` -- added `@officeagent/dw-hr-agent: workspace:^` dependency
14. `modules/message-protocol/src/types/message-type.ts` -- added `HRAgentRequest` and `HRAgentResponse` message types
15. `modules/message-protocol/src/types/digital-workers.ts` -- added `HRAgentRequestPayload` and `HRAgentRequestMessage` types

## Key design decisions

- Followed `dw-marketer-agent` as primary reference (most recent DW agent)
- Used `HRAgentRequest`/`HRAgentResponse` message types (consistent with Marketer/Paralegal/Accountant pattern)
- Included flight overrides for Opus 4.5 and 4.6 (same pattern as dw-paralegal-agent)
- System prompt supports 5 HR scenarios with structured workflows
- RAI guidelines include HR-specific sections on employment law, data privacy, bias/fairness

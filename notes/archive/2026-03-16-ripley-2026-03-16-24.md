# Ripley Learnings — 2026-03-16 (PR-4970916 Review — Duplicate Dispatch Nth+)

## Context
Dispatched to review PR-4970916 (`feat/PL-W009-host-integration-demo`). Applied early bail-out pattern — this is the Nth+ duplicate dispatch for this PR.

## Findings

### Early bail-out confirmed effective again
- PR-4970916 has **51 threads with 32 APPROVE verdicts** and identical 4 commits (`05cbc73`, `b9240c9`, `9e53047`, `8304aad`) since original review (source: ADO REST API `GET /pullRequests/4970916/threads?api-version=7.1`)
- Pre-flight check completed in ~15 seconds vs 5-10 minute full review cycle
- Posted closed-status thread (id: 62213571, status: 4) and resubmitted approve vote (vote: 10)

### Engine consolidation loop still active
- This PR continues to be re-dispatched despite zero new commits and 32+ existing APPROVE verdicts (source: PR-4970916 thread history)
- Root cause: engine consolidation pipeline misclassifies agent-authored bail-out comments as actionable human review findings
- Keywords that trigger false positives: "No Action Required", "early bail-out", "Duplicate Dispatch" (source: prior learnings in knowledge base)

### ADO REST API patterns (confirmed working)
- Thread closure: `POST /pullRequests/{id}/threads?api-version=7.1` with `{"status": 4}` (source: successful call creating thread 62213571)
- VSID lookup: `GET /_apis/connectionData?api-version=6.0-preview` → `authenticatedUser.id` (source: ADO connectionData API)
- Vote submission: `PUT /pullRequests/{id}/reviewers/{vsid}?api-version=7.1` with `{"vote": 10}` (source: successful vote submission)
- Always use `dev.azure.com` hostname, not `office.visualstudio.com` (source: all successful API calls in this session)
- Windows bash requires temp file for JSON payloads: write to `$TEMP/file.json`, use `-d @"$TEMP/file.json"` (source: curl commands in this session)

## Action Items
- Engine dispatch router must implement pre-flight commit SHA + reviewer vote checks before queuing review tasks
- Engine consolidation must filter agent-authored bail-out comments from review findings classification

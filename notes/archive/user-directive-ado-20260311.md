### 2026-03-11: User directive — ADO only, no GitHub
**By:** yemishin (via Claude Code)
**What:** We are using Azure DevOps (ADO) MCP servers for all git operations. We do NOT use GitHub. All repo operations (branches, PRs, commits, work items, code search) must go through `mcp__azure-ado__*` tools.
**Why:** User directive — captured for team memory

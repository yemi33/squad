# Ripley Learnings — 2026-03-16 (PR-4970128 Review — Duplicate Dispatch)

## Task
Review PR-4970128: feat(PL-W015): add CoT streaming and ask-user-question protocol types

## Findings

### Early Bail-Out Applied — No New Code
PR-4970128 commits unchanged since prior reviews:
- `b60eee4e2` feat(PL-W015): add CoT streaming and ask-user-question protocol types
- `ccb74bed4` fix: address PR-4970128 review feedback
- `9f9c2e06b` fix: address review suggestions on PR-4970128

58 threads exist with 34 containing APPROVE verdicts. This is an Nth+ duplicate dispatch with zero new commits. (source: ADO REST API `GET /pullRequests/4970128/threads?api-version=7.1`)

### Engine Consolidation Loop Still Active
The engine continues to re-dispatch review tasks for PR-4970128 despite:
- No new commits since original review
- 34+ APPROVE threads from multiple agents
- Yemi Shin human approval (vote 10)

The consolidation pipeline misclassifies agent-authored bail-out notes as actionable review findings, causing infinite dispatch cycles. (source: PR-4970128 thread history, 58 threads)

### MCP ADO Tools Unavailable
`mcp__azure-ado__*` tools not discoverable via ToolSearch. REST API via `curl` + Bearer token (`az account get-access-token --resource 499b84ac-1321-427f-aa17-267ca6975798`) remains the only working path. Always use `dev.azure.com` hostname. (source: ToolSearch returned no matches)

### ADO REST API Patterns (Confirmed)
- Thread closure: POST `/pullRequests/{id}/threads?api-version=7.1` with `{"status": 4}` (source: successful POST to thread id 62213283)
- VSID lookup: GET `/_apis/connectionData?api-version=6.0-preview` → `authenticatedUser.id` = `1c41d604-e345-64a9-a731-c823f28f9ca8` (source: ADO connectionData API)
- Vote submission: PUT `/pullRequests/{id}/reviewers/{vsid}?api-version=7.1` with `{"vote": 10}` (source: successful PUT returning vote:10)
- Windows temp file pattern: Write JSON to `$TEMP/file.json`, reference via `-d @"$TEMP/file.json"` (source: curl commands in this session)
